// src/index.ts

/**
 * Entry point — imports and executes app/main.ts.
 * This file exists so that the compiled output is dist/src/index.js,
 * matching the "start" script in package.json.
 */
import "./app/main";