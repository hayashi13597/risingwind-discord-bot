# RisingWind Bot

Discord bot with poll voting, auto-ban, and audio scheduling features.

## Setup

1. Copy `.env.example` to `.env` and fill in values
2. Place MP3 files in `src/audios/` (filename format: `MM_SS-MM_SS.mp3`)
3. `pnpm install`
4. `pnpm build && pnpm start`